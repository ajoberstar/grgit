language: java
sudo: required
dist: trusty
install: ""
script: ./travis.sh
branches:
    only:
        - master
jdk:
    - openjdk8
cache:
    directories:
        - $HOME/.gradle/caches
        - $HOME/.gradle/wrapper
